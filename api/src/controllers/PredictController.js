module.exports = {
    async create(req, res){
        const imageData = { imgId, imgName, name, email } = req.body;
        // save no firebase
        // send task

        return res.json(imageData);
    }
}